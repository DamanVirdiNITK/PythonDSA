509. Fibonacci Number

#Method-1 Recursion

    def fib(self, n: int) -> int:
        if n == 0 or n == 1:
            return n
        return self.fib(n-1) + self.fib( n-2)
       
TC = O(2^n)
SC = O(n)
        

#Method-2 Bottom-Up Approach using Tabulation


        
 
 
