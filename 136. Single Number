class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        nums_dict = Counter(nums)
        for key in nums_dict:
            if nums_dict[key] ==1:
                return key
        
        
